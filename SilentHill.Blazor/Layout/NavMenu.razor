<nav class="sh-nav">
    <div class="sh-nav-inner">
        <a class="sh-nav-brand" href="">
            <i class="fa-solid fa-skull"></i>
            <span>Silent Hill</span>
        </a>

        <button class="sh-nav-toggle @(collapseNavMenu ? "" : "active")" @onclick="ToggleNavMenu" aria-label="Menu">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <div class="sh-nav-links @(collapseNavMenu ? "collapsed" : "")">
            <NavLink class="sh-nav-link" href="" Match="NavLinkMatch.All">
                <i class="fa-solid fa-house"></i>
                <span>Home</span>
            </NavLink>
            <NavLink class="sh-nav-link" href="bestiario">
                <i class="fa-solid fa-book-skull"></i>
                <span>Bestiário</span>
            </NavLink>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
